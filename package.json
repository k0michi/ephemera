{
  "name": "ephemera",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "peer",
    "manager",
    "shared"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.59.0",
    "@typescript-eslint/parser": "^8.59.0",
    "eslint": "^9.39.4",
    "eslint-plugin-simple-import-sort": "^13.0.0",
    "eslint-plugin-unused-imports": "^4.4.1"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "gen-all": "npm run gen-migration -w backend && npm run gen-proto -w shared && npm run gen-zod -w shared"
  }
}
